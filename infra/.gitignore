# CDK / Node build artifacts
node_modules/
*.js
*.d.ts
!jest.config.js

# CDK synth output (regenerated by `cdk synth`/`cdk deploy`)
cdk.out/
.cdk.staging/

# Cached context lookups (account-specific: hosted zones, AMIs). Regenerated on demand.
# Commit this instead if you want fully reproducible synths without AWS access.
cdk.context.json
